
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_OR_ANON_KEY=your-anon-or-publishable-key
# For OAuth redirect URLs in local/dev versus prod, Supabase uses the redirect provided by signInWithOAuth.
# Ensure the following URL is whitelisted in your Supabase Auth > URL Configuration > Redirect URLs:
# http://localhost:3000/auth/callback
